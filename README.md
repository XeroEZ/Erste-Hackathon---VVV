# Erste-Hackathon---VVV
Banking application with a chatbot for checks


# Su 3 branche 
	-> Frontend 
	-> Backend 
	-> Android
Pouzivajte tie na ktorych ste pracovali

#Pouzivajte commity ktore majju zmysel 
napr update Headeru
nie banan na mandarink

Pre frontend
	-> git push --set-upstream origin frontend
Pre backend
	-> git push --set-upstream origin backend
pre android
	-> git push --set-upstream origin android


pre nainstalovanie vsetkych potrebnych suborov treba spustit prikaz v terminaly
pip install requirements.txt

nasledne spustenie backendu 
python3 manage.py runserver
